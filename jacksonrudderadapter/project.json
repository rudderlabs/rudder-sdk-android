{
  "name": "jacksonrudderadapter",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "./jacksonrudderadapter/src",
  "targets": {
    "build": {
      "executor": "@jnxplus/nx-gradle:build",
      "outputs": ["./jacksonrudderadapter/build"]
    },
    "lint": {
      "executor": "@jnxplus/nx-gradle:lint",
      "options": {
        "linter": "ktlint"
      }
    },
    "test": {
      "executor": "@jnxplus/nx-gradle:test"
    },
    "ktformat": {
      "executor": "@jnxplus/nx-gradle:ktformat"
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "baseBranch": "master",
        "preset": "conventional",
        "tagPrefix": "${projectName}@"
      }
    },
    "sync-bumped-version-properties": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node gradle-updater.js 'jacksonrudderadapter'"
      }
    },
    "github": {
      "executor": "@jscutlery/semver:github",
      "options": {
        "tag": "jacksonrudderadapter@1.0.0",
        "notesFile": "./jacksonrudderadapter/CHANGELOG_LATEST.md"
      }
    },
    "release-sonatype": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'jacksonrudderadapter-release' && ./gradlew :jacksonrudderadapter:publishToSonatype -Prelease closeAndReleaseSonatypeStagingRepository"
      }
    },
    "snapshot-release": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'jacksonrudderadapter-snapshot' && ./gradlew :jacksonrudderadapter:publishToSonatype"
      }
    }
  },
  "tags": []
}
